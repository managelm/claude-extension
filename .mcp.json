{
  "mcpServers": {
    "managelm": {
      "type": "url",
      "url": "${MCP_URL}",
      "note": "Set MCP_URL to your ManageLM portal MCP endpoint, e.g. https://portal.example.com/mcp. Auth is handled via OAuth 2.0 PKCE — Claude will prompt you to authorize on first connection."
    }
  }
}
